function App(){
  return(
    <div className="App">
        hello~~ 안녕하세요.
    </div>
  );
}

// export 구문이 없어서, index.js가 파일을 찾지 못했을 때 나오는 오류 메세지
// ./src/index.js
// Attempted import error: './App' does not contain a default export (imported as 'App').

export default App;
